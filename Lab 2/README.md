### **Сукманова Анастасия ИТ-1**

### **Лабораторная №2**



#### **Задание 1**



##### **Задача 1**



###### **Текст задачи**



Точка координат.		

Создайте сущность Точка, расположенную на двумерной плоскости, которая описывается:	

* Координата Х: число	
* Координата Y: число	
* Может возвращать текстовое представление вида “{X;Y}”	



Необходимо создать три точки с разными	координатами и вывести	на экран их текстовое представление. 





###### **Алгоритм решения**



Класс Point хранит две координаты x и y, получает их через конструктор и присваивает полям без проверок. Метод toString возвращает строку вида "{x; y}". В Main создаются несколько точек с разными координатами и выводятся на экран



##### **Задача 3**



###### **Текст задачи**



Имена. 

Создайте сущность Имя, которая описывается тремя параметрами: Фамилия, Личное имя, Отчество. Имя может быть приведено к строковому виду, включающему традиционное представление всех трех параметров: Фамилия Имя Отчество (например “Иванов Иван Иванович”). Необходимо предусмотреть возможность того, что какой-либо из параметров может быть не задан, и в этом случае он не учитывается при приведении к текстовому виду. 



Необходимо создать следующие имена: 

* Клеопатра 
* Пушкин Александр Сергеевич 
* Маяковский Владимир 



Обратите внимание, что при выводе на экран, не заданные параметры никак не участвуют в образовании строки.





###### **Алгоритм решения**



Класс Name хранит фамилию, имя и отчество, любое из которых может отсутствовать. Для этого создано три конструктора: 

1. с одним именем вызывает this(null, name, null)
2. с фамилией и именем — this(surname, name, null)
3. с тремя параметрами просто присваивает поля



Метод toString поочерёдно проверяет каждое поле на null: если задано — добавляет в результат с пробелом, если нет — пропускает. Незаданные параметры в строку не попадают. В Main создаются три имени: Клеопатра, Пушкин Александр Сергеевич, Маяковский Владимир.



#### **Задание 2**



##### **Задача 1**



###### **Текст задачи**



Прямая.

Создайте сущность Линия, расположенную на двумерной плоскости, которая описывается: 

* Координата начала: Точка 
* Координата конца: Точка 
* Может возвращать текстовое представление вида “Линия от {X1;Y1} до {X2;Y2}” 



Для указания координат нужно использовать сущность Точка, разработанную в задании 1.1. Создайте линии со следующими характеристиками: 

1. Линия 1 с началом в т. {1;3} и концом в т.{23;8}. 
2. Линия 2, горизонтальная, на высоте 10, от точки 5 до точки 25. 
3. Линия 3, которая начинается всегда там же, где начинается линия 1, и заканчивается всегда там же, где заканчивается линия 2. Таким образом, если положение первой или второй линии меняется, то меняется и третья линия. 
4. После создания всех трех объектов измените координаты первой и второй линий, причем сделайте это таким образом, чтобы положение третьей линии соответствовало требованиям пункта 3. 
5. Измените координаты первой линии так, чтобы при этом не изменились, координаты третьей линии.





###### **Алгоритм решения**



Класс Line хранит два объекта класса Point — start и end, полученные через конструктор. Метод toString возвращает "Линия от " + start + " до " + end — при подстановке объектов Java автоматически вызывает Point.toString, поэтому координаты форматируются сами. 

В Main создаются три линии из четырех точек; для третьей линии в качестве начала и конца используются те же объекты Point, что и у первых двух, а не новые — тогда при изменении исходных точек третья линия тоже изменится.



#### **Задание 3**



##### **Задача 3**



###### **Текст задачи**



Города. 

Создайте сущность Город, которая будет представлять собой точку на карте со следующими характеристиками: 

* Название города
* Набор путей к следующим городам, где путь представляет собой сочетание Города и стоимости поездки в него.



Кроме того, Город может возвращать текстовое представление, в виде названия города и списка связанных с ним городов (в виде пары: “название:стоимость”). 

Используя разработанную сущность реализуйте схему, представленную на рисунке 2.





###### **Алгоритм решения**



Класс City хранит название города и массив путей Path\[]. Класс Path хранит город назначения и цену; его toString возвращает destination.name + ":" + price — именно имя, а не объект целиком, иначе получится бесконечная рекурсия, потому что City.toString снова обратится к Path. 

Конструктор City просто присваивает поля. Метод toString возвращает имя и Arrays.toString(path), где каждый Path приводится своим toString. 

В Main сначала создаются все города только по имени, потому что объекты должны существовать до того, как на них начнут ссылаться, затем у каждого заполняется path массивом new Path\[]{...}, и все города выводятся в цикле. Граф направленный.



#### **Задание 4**



##### **Задача 8**



###### **Текст задачи**



Создаем Города. 

Измените сущность Город из задачи 3.3. Новые требования включают:

* Город можно создать, указав только название
* Город можно создать, указав название и набор связанных с ним городов и стоимостей путей к ним





###### **Алгоритм решения**



Первый конструктор City(String name) вызывает this(name, new Path\[0]), то есть создаёт город с пустым набором путей. Конструктор City(String name, Path\[] path) присваивает оба поля. Вся работа сосредоточена во втором конструкторе, первый лишь делегирует. 

В Main создаётся город G без путей (выведет "G - \[]") и H сразу с путями G:5 и C:3.





#### **Задание 5**



##### **Задача 5**



###### **Текст задачи**



Дроби. 

Создайте сущность Дробь со следующими особенностями:

* Имеет числитель: целое число
* Имеет знаменатель: целое число
* Дробь может быть создана с указанием числителя и знаменателя
* Может вернуть строковое представление вида “числитель/знаменатель”
* Может выполнять операции сложения, вычитания, умножения и деления с другой Дробью или целым числом. Результатом операции должна быть новая Дробь (таким образом,обе исходные дроби не изменяются)



Затем необходимо выполнить следующие задачи:

1. Создать несколько экземпляров дробей.
2. Написать по одному примеру использования каждого метода. 
3. Вывести на экран примеры и результаты их выполнения в формате «1/3 \* 2/3 = 2/9» 4. Посчитать f1.sum(f2).div(f3).minus(5)





###### **Алгоритм решения**



Класс Fraction хранит числитель и знаменатель. В конструкторе проверяется denominator == 0: если да — бросается IllegalArgumentException, иначе поля присваиваются. Благодаря этому после создания знаменатель гарантированно не ноль, и в операциях проверять его уже не надо. 

Метод toString возвращает numerator + "/" + denominator. 

Каждая операция — plus, minus, mul, div — принимает вторую дробь и по формуле вычисляет числитель и знаменатель результата. Пусть первая дробь = a/b, вторая = c/d. Вычисление:

* plus — a\*d + c\*b; b\*d
* minus — a\*d − c\*b; b\*d
* mul — a\*c; b\*d
* div — a\*d; b\*c



Возвращается новый Fraction, исходные дроби не меняются. Для каждой операции есть перегрузка с int: она превращает целое число в new Fraction(value, 1) и вызывает основной метод. Цепочка fr1.plus(fr2).div(fr3).minus(5) возможна потому, что каждый метод возвращает Fraction, у которого снова есть все методы. 

В Main на каждый метод приводится по одному примеру в формате "1/3 \* 2/3 = 2/9".





