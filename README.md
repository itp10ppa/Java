## Сукманова Анастасия ИТ-1,2-2025 БН

## **Лабораторная №1**



### **Задание 1**



#### **Задача 2**



###### Текст задачи



Сумма знаков.

Дана сигнатура метода: public int sumLastNums (int x);

Необходимо реализовать метод таким образом, чтобы он возвращал результат сложения двух последних знаков числах, предполагая, что знаков в числе не менее двух. 

Подсказки:

int x=123%10; // х будет иметь значение 3

int у=123/10; // у будет иметь значение 12

Пример:

x=4568

результат: 14





###### Алгоритм решения



Вход: целое число x

Выход: целое число - сумма двух последних цифр числа



Метод получает целое число x. Находит последнюю цифру числа через остаток от деления на 10 (x % 10). Находит предпоследнюю цифру: сначала делит число на 10 (x / 10), затем берет остаток от деления на 10. Возвращает сумму двух последних цифр.







#### **Задача 4**



###### Текст задачи



Есть ли позитив.

Дана сигнатура метода: public bool isPositive (int x);

Необходимо реализовать метод таким образом, чтобы он принимал число x и

возвращал true, если оно положительное.

Пример 1:

x=3

результат: true

Пример 2:

x=-5

результат: false





###### Алгоритм решения



Вход: целое число x

Выход: true / false



Метод получает целое число x. Проверяет условие x > 0. Возвращает true, если число положительное, и false - если число отрицательное или равно нулю.







#### **Задача 6**



###### Текст задачи



Большая буква.

Дана сигнатура метода: public bool isUpperCase (char x);

Необходимо реализовать метод таким образом, чтобы он принимал символ x и

возвращал true, если это большая буква в диапазоне от ‘A’ до ‘Z’.

Пример 1:

x=’D’

результат: true

Пример 2:

x=’q’

результат: false





###### Алгоритм решения



Вход: символ x

Выход: true / false



Метод получает символ x. Проверяет условие: символ находится в диапазоне от 'A' до 'Z' включительно (x >= 'A' \&\& x <= 'Z'). Возвращает true, если символ является заглавной латинской буквой, и false - если нет.







#### **Задача 8**



###### Текст задачи



Делитель.

Дана сигнатура метода: public bool isDivisor (int a, int b);

Необходимо реализовать метод таким образом, чтобы он возвращал true, если любое из принятых чисел делит другое нацело.

Пример 1:

a=3 b=6

результат: true

Пример 2:

a=2 b=15

результат: false





###### Алгоритм решения



Вход: два целых числа a и b

Выход: true / false



Метод получает два целых числа a и b. Проверяет два условия: делится ли a на b нацело (a % b == 0) или делится ли b на a нацело (b % a == 0). Возвращает true, если выполняется хотя бы одно из условий, и false - если ни одно число не делит другое нацело.







#### **Задача 10**



###### Текст задачи



Многократный вызов.

Дана сигнатура метода: public int lastNumSum(int a, int b)

Необходимо реализовать метод таким образом, чтобы он считал сумму цифр двух чисел из разряда единиц. Выполните с его помощью последовательное сложение пяти чисел и результат выведите на экран. Постарайтесь выполнить задачу, используя минимально возможное количество вспомогательных переменных.

Пример:

5+11 это 6

6+123 это 9

9+14 это 13

13+1 это 4

Итого 4





###### Алгоритм решения



Вход: два целых числа a и b

Выход: целое число - сумма последних цифр



Метод получает два целых числа a и b. Находит последнюю цифру каждого числа с помощью нахождения остатка от деления чисел на 10 и возвращает их сумму. В главном методе последовательно вызывается lastNumSum для пяти чисел и выводится итоговое значение.





\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_





### **Задание 2**



#### **Задача 2**



###### Текст задачи



Безопасное деление.

Дана сигнатура метода: public double safeDiv (int x, int y);

Необходимо реализовать метод таким образом, чтобы он возвращал деление x на y, и при этом гарантировал, что не будет выкинута ошибка деления на 0. При делении на 0 следует вернуть из метода число 0. 

Подсказка: смотри ограничения на операции типов данных.

Пример 1:

x=5 y=0

результат: 0

Пример 2:

x=8 y=2

результат: 4





###### Алгоритм решения



Вход: два целых числа x и y

Выход: ноль или вещественное число - результат деления 



Метод получает два целых числа x и y. Проверяет условие: если y == 0, возвращает 0 (чтобы избежать ошибки деления на ноль). Если y != 0, выполняет деление x на y с приведением к типу double и возвращает результат.







#### **Задача 4**



###### Текст задачи



Строка сравнения.

Дана сигнатура метода: public String makeDecision (int x, int y);

Необходимо реализовать метод таким образом, чтобы он возвращал строку,

которая включает два принятых методом числа и корректно выставленный

знак операции сравнения (больше, меньше, или равно).

Пример 1:

x=5 y=7

результат: “5< 7”

Пример 2:

x=8 y=-1

результат: “8 >-1”

Пример 3:

x=4 y=4

результат: “4==4”





###### Алгоритм решения



Вход: два целых числа x и y

Выход: строка - неравенство с верным знаком



Метод получает два целых числа x и y. Проверяет условия: если x < y, возвращает строку "x < y"; если x > y, возвращает строку "x > y"; если x == y, возвращает строку "x == y".







#### **Задача 6**



###### Текст задачи



Тройная сумма.

Дана сигнатура метода: public bool sum3 (int x, int y, int z);

Необходимо реализовать метод таким образом, чтобы он возвращал true, если два любых числа (из трех принятых) можно сложить так, чтобы получить третье.

Пример 1:

x=5 y=7 z=2

результат: true

Пример 2:

x=8 y=-1 z=4

результат: false





###### Алгоритм решения



Метод получает три целых числа x, y, z. Проверяет условия: равна ли сумма двух чисел третьему (x + y == z, или x + z == y, или y + z == x). Возвращает true, если хотя бы одно из условий выполняется, и false - если ни одна пара чисел в сумме не дает третье число.







#### **Задача 8**



###### Текст задачи



Возраст.

Дана сигнатура метода: public String age (int x);

Необходимо реализовать метод таким образом, чтобы он возвращал строку, в которой сначала будет число х, а затем одно из слов:

год

года

лет

Слово “год” добавляется, если число х заканчивается на 1, кроме числа 11.

Слово “года” добавляется, если число х заканчивается на 2, 3 или 4, кроме чисел 12, 13, 14.

Слово “лет” добавляется во всех остальных случаях.

Подсказка: оператор % позволяет получить остаток от деления.

Пример 1:

x=5

результат: “5 лет”

Пример 2:

x=31

результат: “31 год”

Пример 3:

x=44

результат: “44 года”





###### Алгоритм решения



Метод получает целое число x. Проверяет последнюю цифру числа (x % 10): если она равна 1 и x != 11, возвращает строку "x год"; если последняя цифра равна 2, 3 или 4 и x != 12, 13, 14, возвращает "x года"; во всех остальных случаях возвращает "x лет"







#### **Задача 10**



###### Текст задачи



Вывод дней недели.

Дана сигнатура метода: public void printDays (String x);

В качестве параметра метод принимает строку, в которой записано название дня недели. Необходимо реализовать метод таким образом, чтобы он выводил на экран название переданного в него дня и всех последующих до конца недели дней. Если в качестве строки передан не день, то выводится текст “это не день недели”. Первый день понедельник, последний – воскресенье. Вместо if в данной задаче используйте switch.

Пример 1:

x=”четверг”

результат:

четверг

пятница

суббота

воскресенье

Пример 2:

x=”чг”

результат:

это не день недели





###### Алгоритм решения



Метод получает строку x с названием дня недели. Используя оператор switch, выводит название переданного дня и всех последующих до воскресенья включительно. Если строка не соответствует ни одному дню недели, выводит дэфолтное значение: "это не день недели".





\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_





### **Задание 3**



#### **Задача 2**



###### Текст задачи



Числа наоборот.

Дана сигнатура метода: public String reverseListNums (int x);

Необходимо реализовать метод таким образом, чтобы он возвращал строку, в которой будут записаны все числа от x до 0 (включительно).

Пример:

x=5

результат: “5 4 3 2 1 0”





###### Алгоритм решения



Метод получает целое число x. Создает пустую строку для результата. Далее метод проверяет: число х положительное или отрицательное, для того чтобы определить, в какую сторону пойдет прогрессия. Запускает цикл while. На каждой итерации цикла:

1. добавляет текущее значение x и пробел в строку результата

2\. уменьшает или увеличивает x на 1 в зависимости от того, положительное число х или отрицательное

После завершения цикла возвращает строку, содержащую все числа от исходного x до 0 включительно, разделенные пробелами.







#### **Задача 4**



###### Текст задачи



Степень числа.

Дана сигнатура метода: public int pow (int x, int y);

Необходимо реализовать метод таким образом, чтобы он возвращал результат возведения x в степень y.

Подсказка: для получения степени необходимо умножить единицу на число x, и сделать это y раз, т.е. два в третьей степени это 1\*2\*2\*2

Пример:

x=2

y=5

результат: 32





###### Алгоритм решения



Метод получает два целых числа x (основание) и y (показатель степени). Инициализирует переменную result значением 1. Запускает цикл while, который выполняется, пока y не станет равен 0. На каждой итерации:

1. умножает result на x

2\. уменьшает y на 1

После завершения цикла возвращает значение result, которое равно x в степени y.







#### **Задача 6**



###### Текст задачи



Одинаковость.

Дана сигнатура метода: public bool equalNum (int x);

Необходимо реализовать метод таким образом, чтобы он возвращал true, если все знаки числа одинаковы, и false в ином случае.

Подсказки:

intx=123%10; // х будет иметь значение 3

intу=123/10; // у будет иметь значение 12

Пример 1:

x=1111

результат: true

Пример 2:

x=1211

результат: false





###### Алгоритм решения



Метод получает целое число x. Сохраняет последнюю цифру числа в дополнительную переменную lastNum. Удаляет последнюю цифру (x = x / 10) и запускает цикл while, который выполняется, пока x не станет равен 0. На каждой итерации:

1. сравнивает текущую последнюю цифру с lastNum

2\. если цифры не совпадают, возвращает false

3\. удаляет последнюю цифру (x = x / 10)

Если цикл завершился полностью и все цифры совпали, возвращает true.







#### **Задача 8**



###### Текст задачи



Левый треугольник.

Дана сигнатура метода: public void leftTriangle (int x);

Необходимо реализовать метод таким образом, чтобы он выводил на экран треугольник из символов ‘\*’ у которого х символов в высоту, а количество символов в ряду совпадает с номером строки.

Пример 1:

x=2

результат:

\*

\*\*

Пример 2:

x=4

результат:

\*

\*\*

\*\*\*

\*\*\*\*





###### Алгоритм решения



Метод получает целое число x (высоту треугольника). Использует два вложенных цикла for:

* Внешний цикл: i принимает значения от 0 до x-1 (управляет строками)
* Внутренний цикл: j принимает значения от 0 до i (управляет количеством символов в строке)

На каждой итерации внутреннего цикла выводит символ '\*'. После завершения внутреннего цикла переходит на новую строку. В результате получается треугольник, где в i-й строке выводится i+1 звездочек.







#### **Задача 10**



###### Текст задачи



Угадайка.

Дана сигнатура метода: public void guessGame()

Необходимо реализовать метод таким образом, чтобы он генерировал

случайное число от 0 до 9, далее считывал с консоли введенное пользователем число и выводил, угадал ли пользователь то, что было загадано, или нет. Метод запускается до тех пор, пока пользователь не угадает число. После этого выведите на экран количество попыток, которое потребовалось пользователю, чтобы угадать число.

Пример:

Введите число от 0 до 9:

5

Вы не угадали, введите число от 0 до 9:

9

Вы угадали!

Вы отгадали число за 2 попытки





###### Алгоритм решения



Метод генерирует случайное число от 0 до 9 с помощью Math.random(). Инициализирует счетчик попыток attempts = 0. Запускает бесконечный цикл while (true). На каждой итерации:

1. выводит приглашение ввести число

2\. считывает введенное пользователем число с помощью Scanner

3\. увеличивает счетчик attempts на 1

4\. проверяет, совпадает ли введенное число с загаданным

5\. если совпадает: выводит сообщение "Вы угадали!" и количество попыток, затем выходит из цикла с помощью break

6\. если не совпадает: выводит сообщение "Вы не угадали" и цикл продолжается





\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_





### **Задание 4**



#### **Задача 2**



###### Текст задачи



Поиск последнего значения.

Дана сигнатура метода: public int findLast (int\[] arr, int x);

Необходимо реализовать метод таким образом, чтобы он возвращал индекс последнего вхождения числа x в массив arr. Если число не входит в массив – возвращается -1.

Пример:

arr=\[1,2,3,4,2,2,5]

x=2

результат: 5





###### Алгоритм решения



Метод получает массив целых чисел arr и число x. Инициализирует переменную index = -1. Запускает цикл for, где i принимает значения от 0 до arr.length - 1. На каждой итерации:

1. проверяет, равен ли текущий элемент arr\[i] числу x

2\. если равен, сохраняет текущий индекс в переменную index = i

После завершения цикла возвращает index. Так как проход идет слева направо, переменная index будет содержать индекс последнего найденного элемента, равного x. Если элемент не найден, возвращается -1







#### **Задача 4**



###### Текст задачи



Добавление в массив.

Дана сигнатура метода: public int\[]add (int\[] arr, int x, int pos);

Необходимо реализовать метод таким образом, чтобы он возвращал новый массив, который будет содержать все элементы массива arr, однако в позицию pos будет вставлено значение x.

Пример:

arr=\[1,2,3,4,5]

x=9

pos=3

результат: \[1,2,3,9,4,5]





###### Алгоритм решения



Метод получает массив arr, число x и позицию pos. Создает новый массив newArr размером arr.length + 1. Выполняет три этапа:

1. Первый цикл for (i от 0 до pos - 1): копирует элементы из arr в newArr на те же позиции (newArr\[i] = arr\[i])

2\. Вставляет значение x на позицию pos: newArr\[pos] = x

3\. Второй цикл for (i от pos до arr.length - 1): копирует оставшиеся элементы arr в newArr со сдвигом на 1 вправо (newArr\[i + 1] = arr\[i])

Возвращает новый массив newArr, содержащий все элементы исходного массива с вставленным значением x на позицию pos







#### **Задача 6**



###### Текст задачи



Реверс.

Дана сигнатура метода: public void reverse (int\[] arr);

Необходимо реализовать метод таким образом, чтобы он изменял массив arr.

После проведенных изменений массив должен быть записан задом-наперед.

Пример:

arr=\[1,2,3,4,5]

результат: arr=\[5,4,3,2,1]





###### Алгоритм решения



Метод получает массив целых чисел arr. Использует цикл for, где i принимает значения от 0 до arr.length/2 - 1. На каждой итерации:

1. Сохраняет значение последнего элемента в переменную archieve: archieve = arr\[arr.length - i - 1]
2. Записывает значение i-го элемента на место последнего: arr\[arr.length - i - 1] = arr\[i]
3. Записывает сохраненное значение (archieve) на место i-го элемента: arr\[i] = archieve

Таким образом, элементы меняются местами попарно (первый с последним, второй с предпоследним и т.д.). Цикл проходит только до середины массива, чтобы не менять элементы дважды. Таким образом, массив изменяется на месте, без создания нового.







#### **Задача 8**



###### Текст задачи



Объединение.

Дана сигнатура метода: public int\[] concat (int\[] arr1,int\[] arr2);

Необходимо реализовать метод таким образом, чтобы он возвращал новый массив, в котором сначала идут элементы первого массива (arr1), а затем второго (arr2).

Пример:

arr1=\[1,2,3]

arr2=\[7,8,9]

результат: \[1,2,3,7,8,9]





###### Алгоритм решения



Метод получает два массива arr1 и arr2. Создает новый массив newArr размером arr1.length + arr2.length. Выполняет два цикла:

1. Первый цикл for (i от 0 до arr1.length - 1): копирует все элементы arr1 в начало newArr (newArr\[i] = arr1\[i])
2. Второй цикл for (i от 0 до arr2.length - 1): копирует все элементы arr2 после элементов arr1, используя формулу индекса newArr\[arr1.length + i] = arr2\[i]

Возвращает новый массив newArr, содержащий сначала все элементы первого массива, затем все элементы второго







#### **Задача 10**



###### Текст задачи



Удалить негатив.

Дана сигнатура метода: public int\[] deleteNegative (int\[] arr);

Необходимо реализовать метод таким образом, чтобы он возвращал новый массив, в котором записаны все элементы массива arr кроме отрицательных.

Пример:

arr=\[1,2,-3,4,-2,2,-5]

результат: \[1,2,4,2]





###### Алгоритм решения



Метод получает массив целых чисел arr. Выполняет три этапа:

1. Первый цикл for (i от 0 до arr.length - 1): подсчитывает количество неотрицательных элементов (arr\[i] >= 0) и сохраняет в переменную count
2. Создает новый массив newArr размером count
3. Инициализирует переменную index = 0. Второй цикл for (i от 0 до arr.length - 1): проходит по исходному массиву. Если arr\[i] >= 0, копирует его в newArr на позицию index (newArr\[index] = arr\[i]) и увеличивает index на 1

Возвращает новый массив newArr, содержащий только неотрицательные элементы исходного массива.











